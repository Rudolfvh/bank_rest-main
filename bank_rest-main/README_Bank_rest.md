
# Bank Cards REST API

 Backend-приложение для управления банковскими картами с JWT-аутентификацией, ролевой моделью доступа и REST API.

Приложение реализует безопасную систему управления пользователями и банковскими картами с разделением прав доступа (ADMIN / USER).

# Запуск приложения Локальный (без Docker)

---
## Требования

Установлен Java 17

Установлен Maven 3+

Установлен и запущен PostgreSQL

---
## Шаг 1. Создать базу данных

Создайте базу данных вручную:
```
CREATE DATABASE bankdb;
```
---
## Шаг 2. Настроить подключение к БД

В application.yml или application.properties укажите:

```
spring.datasource.url=jdbc:postgresql://localhost:5432/bankdb
spring.datasource.username=bank
spring.datasource.password=bank
spring.jpa.hibernate.ddl-auto=update
```
---
## Шаг 3. Собрать проект
```
mvn clean package
```
---
## Шаг 4. Запустить приложение

### Вариант 1:
```
mvn spring-boot:run
```
### Вариант 2:
```
java -jar target/bank-rest.jar
```
После запуска приложение будет доступно по адресу:
```
http://localhost:8080
```
Swagger UI:
```
http://localhost:8080/swagger-ui/index.html
```
---

## 2. Запуск через Docker Compose 
## Шаг 1. Собрать проект

```bash
mvn clean package
```

Затем

```bash
docker-compose up --build
```

После запуска:
```
http://localhost:8080
```
Swagger UI:
```
http://localhost:8080/swagger-ui/index.html
```
---

# Основной функционал

---
## Аутентификация и безопасность

* Регистрация пользователей (если реализовано)
* Авторизация по логину и паролю
* Выдача JWT access token
* Stateless-аутентификация
* Ролевая модель доступа (ADMIN / USER)
* Проверка токена на каждом запросе
* Хеширование паролей (BCrypt)
* CORS-конфигурация
* Глобальная обработка ошибок

---

## Пользователи

### USER может:

* Авторизоваться
* Просматривать свои банковские карты
* Запрашивать выпуск новой карты
* Просматривать статус карты
* Блокировать свою карту

### ADMIN может:

* Просматривать всех пользователей
* Просматривать все карты в системе
* Фильтровать карты (по статусу, пользователю и т.д.)
* Блокировать/разблокировать карты
* Управлять статусами карт

---
##  Банковские карты

### Система поддерживает:

* Создание карты
* Получение списка карт
* Пагинацию
* Фильтрацию
* Блокировку карты
* Статусы карты (например: ACTIVE, BLOCKED, EXPIRED)
* Связь карты с пользователем

---
## API Эндпоинты (пример)

---
##  Auth

### POST /auth/login

Получение JWT токена.
```
Request:

{
"username": "admin",
"password": "admin123"
}
```
```
Response:

{
"accessToken": "jwt-token",
"tokenType": "Bearer"
}
```
---

## User API

### GET /user/cards

Получить список своих карт.

### POST /user/cards  
Запросить выпуск новой карты.

### PATCH /user/cards/{id}/block 
Заблокировать свою карту.

---

## Admin API

### GET /admin/cards

Получить список всех карт (с пагинацией).

### PATCH /admin/cards/{id}/block

Заблокировать карту.

### GET /admin/users

Получить список пользователей.

---

# Полная архитектура приложения

Проект реализован по принципам Clean Architecture + Layered Architecture.

## 📂 Структура пакетов

```
com.example.bankcards
│
├── controller        # REST API (входная точка)
│
├── service           # Бизнес-логика
│
├── repository        # Работа с БД (Spring Data JPA)
│
├── entity            # JPA сущности
│
├── dto               # DTO (request / response)
│
├── mapper            # MapStruct мапперы
│
├── security          # JWT, фильтры, UserDetailsService, SecurityConfig
│
├── config            # Swagger, CORS и прочие конфигурации
│
├── util              # Утилиты
│
├── exception         # GlobalExceptionHandler
│
└── BankRestApplication
```

---

## Поток обработки запроса

```
Client
  ↓
Security Filter (JWT)
  ↓
Controller
  ↓
Service
  ↓
Repository
  ↓
Database
```

---

## Слои приложения

### Controller Layer

* Обрабатывает HTTP-запросы
* Валидирует входящие данные
* Возвращает DTO
* Не содержит бизнес-логики

---

### Service Layer

* Основная бизнес-логика
* Проверка прав доступа
* Валидации
* Управление статусами
* Работа с транзакциями

---

### Repository Layer

* Интерфейсы Spring Data JPA
* CRUD операции
* Кастомные запросы

---

### Entity Layer

* JPA-сущности
* Связи между таблицами
* Enum статусов
* Конфигурация ORM

---

### DTO Layer

* Разделение API и Entity
* Request DTO
* Response DTO
* Безопасное представление данных (без паролей)

---

### Mapper Layer

* MapStruct
* Преобразование Entity → DTO
* Минимизация boilerplate-кода

---

### Security Layer

* JwtService — генерация и валидация токена
* JwtAuthenticationFilter — фильтр проверки токена
* CustomUserDetailsService — загрузка пользователя
* SecurityConfig — настройка SecurityFilterChain
* BCryptPasswordEncoder

---

### Exception Layer

* GlobalExceptionHandler
* Обработка:

    * 400 Bad Request
    * 401 Unauthorized
    * 403 Forbidden
    * 404 Not Found
    * 500 Internal Server Error

---

# Безопасность

* Stateless authentication
* JWT access token
* Role-based authorization
* BCrypt hashing
* CSRF отключен (REST API)
* CORS настроен
* Endpoint-level authorization

---

# База данных

Используется:

* PostgreSQL
* JPA / Hibernate

Основные таблицы:

* users
* cards

Связь:

```
User (1) —— (N) Card
```

---

# Тестирование

### Используется:

* JUnit 5
* Mockito
* MockMvc
* WebMvcTest
* Unit тесты сервисов
* Изолированные тесты контроллеров

### Запуск:

```bash
mvn test
```

---

# Docker

Приложение можно запустить в контейнере:

* Dockerfile
* docker-compose (если используется БД)

---

# Документация API

## Swagger UI:

```
http://localhost:8080/swagger-ui/index.html
```

## OpenAPI файл:

```
docs/openapi.yaml
```

---

# Требования

* Java 17
* Maven 3+
* PostgreSQL
* Docker (опционально)

---

# Архитектурные принципы

* SOLID
* Separation of Concerns
* DTO isolation
* Stateless security
* Testable services
* Чистая слоистая архитектура
* Минимальная связанность компонентов

